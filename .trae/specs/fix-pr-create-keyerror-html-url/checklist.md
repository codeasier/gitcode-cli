- [x] git worktree 已创建且分支命名为 `fix/pr-create-keyerror-html-url`
- [x] `pr.py` 中 `default_formatter` 不再使用 `data["html_url"]` 硬编码访问
- [x] 回退逻辑优先级正确: html_url → url → Created PR #{number} → Created pull request
- [x] API 响应包含 html_url 时，输出行为与修改前完全一致
- [x] API 响应缺少 html_url 但包含 url 时，输出 url 值且不崩溃
- [x] API 响应缺少 html_url 和 url 但包含 number 时，输出 `Created PR #<number>` 且不崩溃
- [x] API 响应缺少 html_url、url 和 number 时，输出 `Created pull request` 兜底信息
- [x] 新增测试用例全部通过（含兜底场景测试）
- [x] 原有测试用例全部通过（无回归）
- [x] `python -m ruff check src/ tests/` 通过
- [x] `python -m ruff format --check src/ tests/` 通过
- [x] `python -m basedpyright src/` 通过
- [x] 代码已提交到 `fix/pr-create-keyerror-html-url` 分支
- [x] PR 已通过 `gh pr create` 创建，使用项目 PR 模板，关联 Fixes #13
- [x] 检视意见已修复：最终 fallback 不再输出 `Created PR #?`，改为 `Created pull request`
