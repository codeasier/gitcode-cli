- [x] `safe_echo()` 函数在 `utils.py` 中实现，对 Unicode 编码错误使用 `errors='replace'` 策略
- [x] `cli.py` 入口处添加 `sys.stdout`/`sys.stderr` 编码重配置（`reconfigure(encoding='utf-8', errors='replace')`）
- [x] 编码重配置使用 try/except 包裹，失败时不影响程序启动
- [x] 所有 `click.echo()` 调用替换为 `safe_echo()`（issue.py、pr.py、auth.py、formatters.py、cli.py）
- [x] `safe_echo` 单元测试覆盖：正常 ASCII 文本、emoji 文本、GBK 不可编码字符
- [x] `formatters.py` Unicode 输出测试覆盖：包含 emoji 的 issue/PR 格式化输出
- [x] `cli.py` 入口编码配置测试覆盖：reconfigure 调用、fallback 行为
- [x] 所有单元测试通过（`python -m pytest tests/unit/`）
- [x] Lint 通过（`python -m ruff check src/ tests/`）
- [x] 格式检查通过（`python -m ruff format --check src/ tests/`）
- [x] 类型检查通过（`python -m basedpyright src/`）
- [x] 测试覆盖率 >= 90%
- [x] 在独立 git worktree 中开发，分支名为 `fix/issue-14-windows-unicode-encode`
- [x] PR 使用项目 PR 模板提交，关联 `Fixes #14`
- [x] 使用 `gh pr create` 命令提交 PR
